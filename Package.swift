dependencies: [
    // Other dependencies remain unchanged
],
targets: [
    .target(
        name: "EyeLube",
        dependencies: [
            // Rest of dependencies
        ]
    ),
    .target(
        name: "PreferencesStorage",
        dependencies: [
            // Rest of dependencies
        ]
    )
]